import PublicProvider from "../PublicProvider";

function Login() {
  return (
    <PublicProvider>
      <div>
        <h1 className="text-black">Login</h1>
      </div>
    </PublicProvider>
  );
}

export default Login;
